The Cases I Have Run:
  a.if the input coordinates is beyond (1,1) to (3,3) or the coordinate is not in the form of "x,y", or other strings rather than "quit", it will repeatedly prompt the user to enter a coordinate in form of "x,y(x and y are from 1 to 3)"
  b.if the input coordinate has already been occupied, it will tell the user the input is invalid and ask for another valid input.
  c.I tried to enter the wrong file name instead of "TicTacToe", it will tell the user the name of game is wrong and should be "TicTacToe"
  d.If I only enter the program name, it will print out the user message that tells the user the input should be "Lab3.exe TicTacToe"
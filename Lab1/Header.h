#pragma once
#include <iostream>
#include <vector>
#include <fstream>
#include <sstream>
#include <string>
#include <cstring>

enum names { Lab1, Header, argc };
enum values { success, fail_to_open, read_write_error, wrong_number_of_command_line_argument };
